# Run this in the API server shell to verify tables exist:

python3 -c "import psycopg2,os; c=psycopg2.connect(os.getenv('DATABASE_URL')); r=c.cursor(); r.execute('SELECT table_name FROM information_schema.tables WHERE table_schema=\'public\' ORDER BY table_name'); tables=[t[0] for t in r.fetchall()]; print('Tables found:', tables); print('Count:', len(tables)); c.close()"

# Expected output should show 7 tables:
# ['api_costs', 'article_topics', 'articles', 'corpus_topics', 'entity_relationships', 'extraction_results', 'facts']
