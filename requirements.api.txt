# Vermont Signal V2 - API Server Dependencies
# Minimal dependencies for FastAPI REST API (no ML models needed)

# API Framework
fastapi==0.115.0              # REST API framework
uvicorn[standard]==0.32.0     # ASGI server for FastAPI

# Rate Limiting
slowapi>=0.1.9                # Rate limiting for FastAPI

# Database
psycopg2-binary==2.9.10       # PostgreSQL driver

# Configuration
python-dotenv>=1.0.0          # Environment variable management

# That's it! API server only serves data from PostgreSQL.
# ML processing happens in the worker.
